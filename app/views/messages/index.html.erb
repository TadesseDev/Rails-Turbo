<p style="color: green"><%= notice %></p>
<%= Time.now%>
<h1 id="messages_counter">Messages <%= @messages.length%></h1>
<div id="new_message">
<%= render "form", message: Message.new %>
</div>
<div id="messages">
  <% @messages.each do |message| %>
  <div id="<%= dom_id message%>">

    <%= render message %>
  </div>
  <% end %>
</div>

<%= link_to "New message", new_message_path %>
